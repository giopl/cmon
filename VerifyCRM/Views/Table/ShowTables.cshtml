@model IEnumerable<VerifyCRM.Models.app_table>

@{
    ViewBag.Title = "Show Tables";
}

<h2>Show Tables</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>Type</th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.updated_on)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@item.table_type</td>
        <td>
            <a href="@Url.Action("TableDetails", new { id = item.id })">
                @Html.DisplayFor(modelItem => item.fulltablename)
            </a>
        </td>        
        
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.updated_on)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditTable", new { id=item.id })
            
        </td>
    </tr>
}

</table>
